[tool.pytest.ini_options]
pythonpath = ["."]
#addopts = "--cov=. --cov-report=term-missing"
testpaths = ["tests"]

[tool.coverage.run]
omit = ["tests/*", "harry.py"]

[tool.coverage.report]
fail_under = 80
